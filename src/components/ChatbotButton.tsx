// Chatbot eliminado temporalmente para evitar duplicados
import React from 'react';

const ChatbotButton: React.FC = () => {
  // Componente deshabilitado - chatbot se maneja desde WhatsApp
  return null;
};

export default ChatbotButton;